This marker declares the embedded MultiDetectGCS source root inside the unified
Multi-Detect repository. Tooling uses it to stop repository-root discovery here
instead of walking up to the parent repository's .git directory.
